import 'package:flutter/material.dart';

class CustomColors {
  CustomColors._();

  static const primaryColor = Color(0xFFFFED00);

  static const myMessageColor = Color.fromRGBO(13, 110, 255, 1);

  static const titleText = Colors.white;

  static const settingText = Color.fromRGBO(255, 255, 255, 0.8);

  static const paragraphTextColor = Color.fromRGBO(255, 255, 255, 0.6);

  static const contributionTextColor = Color.fromRGBO(255, 255, 255, 0.4);

  static const borderColor = Color.fromRGBO(255, 255, 255, 0.2);

  static const buttonPanel = Color.fromRGBO(255, 255, 255, 0.1);

  static const modalBackground = Color(0xFF1D1F23);

  static const danger = Color(0xFFFF2D55);

  static const snackBackground = Color(0xB8202631);
  
  static const blockColor = Color.fromRGBO(255, 45, 85, 0.2);

  static const positive = Color(0xFF00B347);

  static const userColors = [
    Color(0xFFFFFF99),
    Color(0xFF00B247),
    Color(0xFFEF5DA8),
    Color(0xFFFFED00),
    Color(0xFFFFFFFF),
    Color(0xFFFF6B5C),
    Color(0xFFFF6682),
    Color(0xFFDDA6FF),
    Color(0xFFA1A3FF),
    Color(0xFF69A5FF)
  ];
}
